# API

`POST /auth/register`, `POST /auth/login`, `GET /auth/me`, `POST /auth/logout`, `POST /auth/forgot-password`, `POST /auth/reset-password`, and `GET /dashboard` are available. Send `Authorization: Bearer <token>` for protected routes.
