# Database

Prisma models `Company`, `User`, `Project`, `Task`, and `AuditLog` form the Milestone 1 schema. Company-scoped unique constraints prevent cross-company ambiguity. The audit log intentionally excludes secrets.
